<%= form_for(recipients_list) do |f| %>
  <% if recipients_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipients_list.errors.count, "error") %> prohibited this recipients_list from being saved:</h2>

      <ul>
      <% recipients_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
